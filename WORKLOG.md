# Work Log

...Updated Section...

The Computer folder now uses shell:MyComputerFolder instead of FOLDERID_ComputerFolder.

- functional via `SHGetKnownFolderPath` (Documents, Pictures, Music, Downloads) and shell target for virtual folders (Computer → `shell:MyComputerFolder`) plus...